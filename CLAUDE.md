# ウィジェットRPG — 開発ガイド / 現状仕様

ホーム画面のウィジェットで冒険を見守る放置RPG(Swift + SwiftUI + WidgetKit、iOS 17+)。
このファイルは**現状の仕様と決定事項**をまとめたもの。数値の根拠はすべて実装済みコード。

---

## 1. プロジェクトの扱い方

### ビルド・実行
```bash
xcodegen generate          # project.yml から .xcodeproj を生成(gitignore対象)
# シミュレータ
xcodebuild -project WidgetRPG.xcodeproj -scheme WidgetRPG \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' \
  -derivedDataPath build CODE_SIGNING_ALLOWED=NO build
# 実機(shota-iPhone / iPhone 16)
xcodebuild ... -destination 'id=00008140-0012246E3EDA801C' \
  -derivedDataPath build-device -allowProvisioningUpdates build
xcrun devicectl device install app --device 00008140-0012246E3EDA801C \
  build-device/Build/Products/Debug-iphoneos/WidgetRPG.app
```

- ターゲットは **WidgetRPG(アプリ)** と **WidgetRPGWidget(拡張)**。`Shared/` は両方にコンパイルされる
- **アセットやファイルを追加したら `xcodegen generate` を必ず実行**(しないとビルドに含まれない)
- 署名: `Signing.xcconfig`(DEVELOPMENT_TEAM=7Z2ZRB6V2J、自動署名)
- 保存先: App Group `group.com.shota.widgetrpg` の JSON(`save.json`)。**セーブ形式を変えると旧セーブは初期化される**(開発中は許容)

### 注意点
- SourceKit は他ファイルの型を解決できず「Cannot find type ...」を大量に出す。**ビルドが通れば無視してよい**
- 実機/シミュレータでアセットが古いまま出るときは `rm -rf build` してクリーンビルド
- 作業ディレクトリにアクセスできなくなる場合(macOS TCC)は、GitHub からスクラッチパッドにクローンして作業 → push → 復帰後に `git pull`

### 検証ツール
| ツール | 用途 |
|---|---|
| `Tools/Playtest/run.sh` | ヘッドレス自動プレイ。`CASUAL=1` で「1日4回起動するプレイヤー」を模擬 |
| `Tools/AssetPipeline/main.py` | AI画像 → ドット絵アセット化(後述) |
| レンチメニュー(ホーム右上) | 戦闘バランス調整・ガチャ検証・卵検証・デバッグ設定・ウィジェット確認 |

開発フック(環境変数、シミュレータは `SIMCTL_CHILD_` を付ける):
`START_TAB` / `DEV_BATTLE` / `DEV_BATTLE_AUTO="swordsman,akuma"` / `DEV_BATTLE_ELEM` /
`DEV_GACHA` / `DEV_EGG` / `DEV_CHAR_DETAIL` / `DEV_INVENTORY` / `DEV_WIDGET` / `DEV_WIDGET_OFF` / `DEV_RICH` / `DEV_QUICK`

---

## 2. UIの方針

- 暗めのテーマ(紺+金)。**ひらがなを多用せず漢字表記**
- 一覧は**横4列のカードグリッド**(キャラ・オトモ・装備・パーティで統一)
- **タップ=選択/装備、長押し=詳細**(装備・パーティ)
- パネルはドット絵フレーム(`ui_frame`、9-slice)。`UITheme.usePixelPanelFrame = false` で従来の角丸に戻せる
- ホームは**タブバー以外の全面が村シーン**。`UITheme.useVillageHome = false` で旧リストに戻せる

### ドット絵の表示ルール(重要)
ドット絵を半端な倍率で拡大すると、あるドットは4px・隣は5px…とバラついて直線がガタつく。
**必ず `PixelArtImage` / `PixelSnap`(Shared/UI)を使い、1ドット=整数個の画面ピクセルに丸めて表示する**。
建物アセットは 1px = 1ドットで保存している(丸めの刻みを細かくするため)。

### 村シーン(ホーム・ウィジェット共用)
`Shared/UI/VillageSceneView.swift`
- 背景 `village_bg_portrait` + 建物レイヤー(`village_tent` / `village_shop` / `village_guild`)
- 位置と大きさは**枠に対する割合**(`VillageLayout.portrait` / `.wide`)。画面比が変わっても破綻しない
- **状態で見た目が変わる**:
  - ギルド: スカウトできる時だけ窓に人影(`village_guild_people` を重ねる)+「!」バッジ。できない時は暗く沈む
  - ショップ: 珍しい(低確率)入荷中は金の輝き+「珍」バッジ
- タップ: テント→持ち物 / ショップ→購入 / ギルド→スカウト(ウィジェットではタップ無効)

---

## 3. ウィジェット

- 対応サイズ: **中(systemMedium)・大(systemLarge)**。`contentMarginsDisabled()` で村シーンを全面表示
- 画面は5つを「次へ」で巡回: **拠点(村) → 卵 → 攻略 → ショップ → ステータス**
  - 拠点を先頭にしてウィジェットの顔にしている。アプリ起動時に `WidgetScreenStore.current = .base` へ戻す
- ヘッダに画面名・ページドット・時間帯/天候・**更新⟳と次へ›のコンパクトボタン**(表示領域を最大化するためフッターには置かない)
- 更新は15分刻みのタイムライン+ボタンによる即時更新

### iOS側の制約(守る)
- 操作は**ボタン/トグルのみ**(スクロール・スワイプ・タップ座標は不可)
- タイムライン更新はシステム予算内(1日40〜70回程度)。能動的な更新はボタンに寄せる

### 未実装
ディープリンク(タップでアプリの該当画面へ)、WeatherKit連携(天候はダミー)、ゲリラのウィジェット通知

---

## 4. ゲームシステム(確定仕様)

### パーティ
**メインキャラ1人 + オトモ2匹**。防具は1個のみ装備。

### 戦闘(ATB + スロット)
```
ゲージ増加 = deltaTime × (50 + 実効速度/2) / 60      # 速度は逓減式
ダメージ   = 参照ステ × 威力% × 250/(250+防御)
             × 属性倍率 × 会心2倍 × 異常の敵1.3 × 弱体化1.2 + 固定ダメージ
```
- **防御は割合軽減**(引き算式は閾値超えで実質無敵になるため廃止)。防御+120 ≒ HP+240 の等価交換
- **敵は防御ステータスを持たない**(味方のみ軽減)
- 行動ごとに 0.6 秒の間を置き、足元にスキル名・対象の頭上にダメージを出す
- スロットを一定周回で必殺技。**必要周回は威力連動**(≤250%=2周 / ≤330%=3周 / それ以上=4周、敵味方共通)
- 戦闘中に味方をタップで**簡易詳細**(ステータス・スロット・NEXT・必殺進捗・装備・パッシブ)。表示中は戦闘が一時停止

### 属性(4すくみ+闇)
```
風 → 電気 → 水 → 炎 → 風   有利1.3倍 / 不利0.85倍
闇: 全属性に与ダメ1.2倍、かつ全属性から被ダメ1.2倍(闇同士は1.44倍)
```

### 状態異常(行動終わりに解除判定)
| 異常 | 効果 | 解除率 |
|---|---|---|
| 毒 | 現HPの5%/行動 | 20% |
| 火傷 | 自攻撃の25%/行動 | 30% |
| 洗脳 | スロットが50%で通常攻撃に | 40% |
| 逆光 | スロットが逆回転(周回が進まない) | 40% |
| 弱体化 | 被ダメ×1.2 | 30% |
| 攻撃低下 | 攻撃×0.7 | 30% |
| 速度低下 | 速度×0.7 | 40% |

### 成長・進化
```
必要経験値 = キャラ 5×Lv² / オトモ 4×Lv²      # 累計 ∝ Lv³(ポケモン式)
取得経験値 = 毎分 4〜8 + 推奨Lv×2(ボス発見後は30%)
キャラ: 進化倍率 ×1.0 / ×1.25 / ×1.5
```
- **習得テーブル**: Lv10 スキル / **Lv25 進化(第一必殺技)** / Lv30 パッシブ /
  **Lv45 進化(第二必殺技)** / Lv60 パッシブ / Lv70 スキル / Lv80 パッシブ
- 進化コスト: **第一15個 / 第二30個**の属性石。通常キャラは自属性、特殊キャラはどの石でも可
- アンカー: **剣士 Lv50 最終進化 = HP600 / 攻200 / 防150 / 速100 / 魔30**
- オトモ: 進化 Lv20 / Lv40(素材不要)。**合成**=同種同星2体消費で星+1(スキルは変化しない)
- オトモのステータス = 同レベルの標準メインキャラ合計の
  **星1:60% / 星2:65% / 星3:70% / 伝説・神話:80%**(未進化は1段階につき−5%)。種族比率で配分+個体値±10%

### スキル・パッシブ
- **メインキャラは職ごとの固定キット**(`Shared/GameData/JobKits.swift`、全22職)。抽選しない
- **オトモ・装備は抽選**(`Shared/GameData/SkillCatalog.swift`)。
  レア度は **普通70% / やや珍しい25% / 珍しい5%**。
  「先にレア度を決め、その中で均等に選ぶ」方式なので**エントリを増やしても排出率は変わらない**
- パッシブは**全15種が戦闘で機能する**(二回行動・背水の陣・固定ダメージ・オトモ強化・吸血・反撃・状態異常耐性を含む)
- スキルは対象(単体/全体/ランダム2体)・状態異常付与・割合回復・吸血・追い討ちを表現できる
- 新スキル/パッシブは**カタログの該当プールに1行足すだけ**で抽選対象になる

### ダンジョン・経済
- メイン4系統×15層。**最終ボスの推奨Lv: ルルイエ30 / 無貌45 / 混沌65 / 禁書75**
- 敵は「推奨Lv45相当」を基準に `推奨Lv/45` でスケール(速度のみ緩やか)。
  **アンカー: 無貌の回廊の最終ボス = 調整シミュレーターのヒュドラと同格**
- ボス発見後の自動探索は**最大5時間**(以降は収集が止まる)
- 10分ごとに1回ドロップ: 素材40% / コイン43% / 装備10% / 卵7%
  (量は潜行中ダンジョンの推奨Lv比例)
- **属性石の祠**(5属性・推奨Lv20・メイン5層クリアで解禁): 石50%(1〜2個)/コイン25/素材15/装備10
- ショップは石経済が基準(石1個≒100コイン)。ギルドは出現率 基本70/特殊25/レア5%、
  **所持キャラは出現せず残りに再分配**。スカウト率はキャラごと独立(基本25%+5/回・特殊10%+4/回・レア30%固定)
- 卵: 普通2h / 珍しい5h / 伝説10h。孵化器は1個ずつ
- **モンスターテイマー**(ルルイエ海淵クリアで加入): 加入だけで**普通の卵は即時孵化**、
  編成して潜入すると珍しい/伝説の孵化が2倍速

### ゲリラクエスト
ランダム出現(平均6〜7時間・2時間で消滅)の即ボス戦。難易度は現在の推奨Lv+5。
**現在ホーム画面からは非表示**(コードは残置)。

---

## 5. バランスの現状と課題

計測(`Tools/Playtest/run.sh`):
- **カジュアル(1日4回起動)で全クリア 約19〜23日**(目標の2〜3週に収まっている)
- クリア時レベルが推奨曲線にほぼ一致(オーバーレベル問題は解消済み)

**残課題**(未対応):
1. 職バランス: 侍が突出、魔法職と一部物理職は**耐久不足で勝率0%**(メイン1人制で紙耐久が致命的)
2. 敗北ペナルティがない(連敗しながらレベリングが最適解になりうる)
3. 素材が終盤余る(装備強化が3段階で頭打ち)
4. 速度低下の効果値が逓減式で弱くなった

**進め方の方針**(ユーザー合意): ①ルール(土台)の修正 → ②スキル・パッシブの中身を揃える → ③数値と敵の手応えを調整。
①は完了、現在②の途中。

---

## 6. アセットパイプライン(`Tools/AssetPipeline`)

AI生成画像をドット絵アセットに整える。設定は `config.json` に集約。
```bash
.venv/bin/python main.py run --category characters --files input/characters/xxx.png
```

処理順(**ドット化を最後の形状操作にする**のが鉄則):
```
background_remover → noise → isolate → trim → resize → repair → ground → outline → pixelize → palette
```
- `pixelize` はキャンバス(0,0)基準の `pixel_size×pixel_size` ブロック量子化。**サイズを変えない**
- **ドット化の後にリサイズや穴埋めを入れるとグリッドが崩れる**(縁だけギザギザになる)
- 現在の設定: `pixel_size=3` / `outline_thickness=3` / `palette_size=32` / `repair_close=0`
  (repair は隣の色を借りて滲むため無効。ブロック量子化が細い刃を自然に繋ぐ)
- 共通パレット `palette/common_32.json` をピン留めして色調を揃える
- 生成時の推奨: **単色背景(マゼンタ等、キャラと色相が違う色)/装飾を重ねない/足を接地**

### アセットの現状
| 種類 | 実物 | 必要 |
|---|---|---|
| キャラ | 剣士・魔法使い(待機)、悪魔(全7状態)、天使(待機) | 22職 |
| オトモ | なし(コード描画のプレースホルダー) | 43種 |
| 敵 | なし | 13体 |
| 建物・背景 | 拠点テント・ショップ・ギルド(+人影)・背景 | — |
| UI | ドット絵フレーム | — |
| アプリアイコン | **なし** | 1 |
| BGM・効果音 | **なし** | — |

命名規則: `<spriteKey>_<state>_<frame>`(例 `swordsman_idle_0`)。
`spriteKey` は職ID / オトモ種族ID / 敵ID。画像がなければコード描画のプレースホルダーにフォールバックする。

---

## 7. コミットの作法

- コミットメッセージは日本語。**何を・なぜ変えたか**を書く(数値変更は根拠と計測結果も)
- 末尾に付ける:
  ```
  Co-Authored-By: Claude <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_...
  ```
- 実装 → ビルド → (可能なら)スクリーンショットや自動プレイで検証 → コミット → プッシュ → 実機インストール
